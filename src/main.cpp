#include <rbtree.h>
#include <vector>
#include <iostream>
using namespace std;

int main() {
    rdtree::node<int> a;
    rdtree::node<int> b(1);
    rdtree::rdtree<int> tree;

    
    vector<int> vec;
    vec.~vector<int>();

    cout << vec.size() << endl;
}
